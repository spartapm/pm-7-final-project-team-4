# 이벤트 트래킹 명세서

#### (1) 온보딩

| 화면 ID | 이벤트명 | 트리거 시점 | 주요 파라미터 (값 형식) | 매핑 지표 |
| --- | --- | --- | --- | --- |
| Onboard-01 | `sign_up_start` | 카카오톡으로 시작하기 클릭 | - | 가입 퍼널 시작점, 이탈률 분석 |
| Onboard-01 | `sign_up`  | 카카오 인증 성공 후 Onboard-02 진입 | `method`(`kakao`) | **핵심 KPI 분모** - 전체 신규 가입 사용자 수 |
| Onboard-01 | `sign_up_fail` | 카카오 인증 실패/취소 | `fail_reason`(`auth_cancel` / `auth_error` / `network`) | 가입 단계 이탈 원인 진단 |
| Onboard-02 | `onboarding_pet_type_select` | 강아지/고양이 카드 선택 | `pet_type`(`dog` / `cat`) | 반려동물 유형별 가입자 구성, 완료율 분석 |
| Onboard-02 | `onboarding_complete` | 완료하기 클릭 후 데이터 저장 성공, Home-01 진입 | `pet_type`(`dog` / `cat`), `journey_type`(`before` / `after`) | 온보딩 완료율, 생전·사후 가입 비중 분석 |
| Onboard-02 | `onboarding_error` | 완료하기 클릭 시 유효성 검증 실패 | `error_field`(`species` / `name` / `journey`) | 온보딩 입력 필드별 이탈 원인 진단 |

#### (2) 홈 / 네비게이션

| 화면 ID | 이벤트명 | 트리거 시점 | 주요 파라미터 (값 형식) | 매핑 지표 |
| --- | --- | --- | --- | --- |
| Home-01 | `home_view` | 홈 화면 진입 | `journey_type`(`before` / `after`) | 홈 화면 진입률, 생전·사후 이용 비중 |
| Home-01 | `home_node_click` | 완료 노드 클릭 
→ Memory-02 이동 | `node_index`(1~5), `item_id` | 완료 기록 재조회 빈도 분석 |
| Nav-01 | `nav_tab_click` | 하단 탭 클릭 | `tab_name`(`home` / `list` / `memory` / `profile`) | 탭별 방문 빈도, 주요 이용 기능 분석 |

#### (3) 리스트 (조회/생성/수정/삭제)

| 화면 ID | 이벤트명 | 트리거 시점 | 주요 파라미터 (값 형식) | 매핑 지표 |
| --- | --- | --- | --- | --- |
| List-01-01/02 | `list_view` | 리스트 화면 진입 (항목 유무 무관) | `item_count`(정수), `journey_type`(`before` / `after`) | 보조지표 A·B의 분모 |
| List-01-01 | `list_add_button_click` | (+) 버튼 클릭 | `journey_type`(`before` / `after`) | 보조지표 B(리스트 추가 클릭률)의 분자 |
| List-01-03 | `list_item_create` | 신규 항목 **데이터 저장 성공**
 (체크 아이콘/엔터/포커스아웃은 저장을 유발하는 사용자 동작일 뿐, 이벤트 발생 기준은 저장 성공임) | `item_id`, `journey_type`(`before` / `after`) | 리스트 생성 퍼널: 작성 완료 전환율 |
| List-01-01 | `list_item_edit` | 항목명 수정 저장 성공 | `item_id` | 리스트 재편집 빈도(참고용) |
| List-01-01 | `list_item_delete` | 항목 삭제 성공 | `item_id` | 리스트 삭제 빈도(참고용, 콘텐츠 매력도 반증 신호) |
| List-01-01 | `list_item_row_click` | 리스트 항목 행 클릭 → List-02 진입 | `item_id` | 리스트 → 기록 화면 진입 분석 |

#### (4) 기록 작성 및 수정

| 화면 ID | 이벤트명 | 트리거 시점 | 주요 파라미터 (값 형식) | 매핑 지표 |
| --- | --- | --- | --- | --- |
| List-02 | `record_edit_view` | 기록 수정 화면 진입 | `entry_point`(`list`) | 보조지표 A(기록화면 진입률)의 분자 |
| List-02 | `record_field_interact` | List-02 진입 후 각 필드(날짜/제목/우리의 이야기/사진)에서 최초 유효 인터랙션 발생 시 1회만 전송. 
동일 필드 재클릭·재입력 시 재전송하지 않음 | `field_name`(`date` / `title` / `story` / `photo`) | **선행지표**: 필드별 관여도, 완료 예측 신호 |
| List-02 | `record_photo_add` | 사진 추가 성공 | `photo_count`(정수, 1~5) | 기록 콘텐츠 풍부도(참고용) |
| List-02 | `record_photo_upload_fail` | 사진 업로드 실패 | `fail_reason`(`network` / `size` / `format`) | 사진 업로드 실패 원인별 모니터링 |
| List-02 | `record_temp_save` | 임시저장 클릭 후 저장 성공 | - | 임시저장 후 미완료 사용자 병목 진단 |
| List-02 | `record_temp_save_fail` | 임시저장 실패 | `fail_reason`(`network` / `server`) | 임시저장 실패 모니터링 |
| List-02 | `record_complete` | List-02에서 완료 버튼 클릭 후 데이터 저장 성공 → List-03-01 진입 | `journey_type`(`before` / `after`), `has_photo`(`true` / `false`), `char_count`(정수) | **핵심 KPI 분자** - 기록 완료 사용자 수 |
| List-02 | `record_complete_fail` | List-02에서 완료 버튼 클릭 후 저장 실패 → List-03-02 진입 | `fail_reason`(`network` / `server` / `validation`) | 완료 실패 원인 진단 |
| List-03-01 | `record_complete_modal_action` | 완료 모달 버튼 선택 | `next_action`(`to_list` / `to_memory`) | 완료 직후 행동 분기 분석
(리스트 재작성 vs 회고) |
| List-03-02 | `record_retry_click` | '다시하기' 클릭 | `retry_count`(정수) | 저장 실패 후 재시도 행동 분석 |
| List-03-03 | `record_exit_modal_action` | ‘나가기’ 모달 버튼 클릭 | `item_id` |  |
| List-03-03 | `record_complete_modal_action` | ‘저장하고 나가기’ 모달 버튼 클릭 | `item_id` | 저장 수 빈도 |

#### (5) 메모리

| 화면 ID | 이벤트명 | 트리거 시점 | 주요 파라미터 (값 형식) | 매핑 지표 |
| --- | --- | --- | --- | --- |
| Memory-01-01/02 | `memory_view` | 메모리 화면 진입(완료기록 유무 무관) | `card_count`(정수), `journey_type`(`before` / `after`) | 메모리 화면 진입, 회고 행동 빈도 |
| Memory-01-01 | `memory_sort_change` | 정렬 옵션 변경 | `sort_type`(`latest` / `oldest`) | 정렬 기능 이용 빈도 (참고용) |
| Memory-01-01 | `memory_card_click` | 메모리 카드 클릭 → Memory-02 진입 | `item_id` | 완료 기록 재조회(회고 재방문) 빈도 |
| Memory-01-01 | `memory_card_delete` | 카드 ****삭제 성공 | `item_id` | 완료 기록 삭제 빈도(참고용) |
| Memory-02 | `memory_detail_view` | 기록 상세 화면 진입 | `entry_point`(`home` / `memory`) | 기록 상세 조회(회고 완결 행동), 진입 경로별 비교 |
| Memory-02 | `memory_edit_click` | 수정 버튼 클릭 → Memory-03 재진입 | `item_id` | 완료 기록 재편집 빈도 |
| Memory-03 | `memory_edit_view` | 메모리에서 기록 수정 화면(Memory-03) 진입 | `item_id` |  |
| Memory-03 | `memory_photo_add` | 사진 추가 성공 | `mem_photo_count`(정수, 1~5) | 기록 콘텐츠 풍부도(참고용) |
| Memory-03 | `memory_photo_upload_fail` | 사진 업로드 실패 | `mem_fail_reason`(`mem_network` / `mem_size` / `mem_format`) | 사진 업로드 실패 빈도 수(사용성) |
| Memory-03 | `memory_complete` | Memory-03에서 ‘완료’ 버튼 클릭 → List-03-01(모달) 표출 | `item_id` | 기록 최초 게시 후 수정 빈도 |
| Memory-03 | `memory_complete_fail` | Memory-03에서 완료 버튼 클릭 후 저장 실패 → List-03-02(모달) 표출 | `fail_reason`(`network` / `server` / `validation`) | 완료 실패 원인 진단 |
| Memory-03-01 | `memory_exit_modal_action` | ‘나가기’ 버튼 클릭 | `item_id` |  |
| Memory-03-01 | `memory_complete_modal_action` | ‘저장하고 나가기’ 버튼 클릭 | `item_id` | 저장 확정 빈도 수 |

#### (6) 프로필 / 계정 관리

| 화면 ID | 이벤트명 | 트리거 시점 | 주요 파라미터 (값 형식) | 매핑 지표 |
| --- | --- | --- | --- | --- |
| Profile-01 | `profile_view` | 프로필 화면 진입 | `journey_type`(`before` / `after`) | 프로필 화면 진입 빈도 |
| Profile-02 | `profile_edit_complete` | 프로필 수정 저장 성공 | `changed_fields`(`name` / `age` / `species`) | 프로필 수정 행동 (참고용) |
| Profile-03-01 | `journey_switch_confirm` | 생전↔사후 전환 변경 성공 | `from_type`(`before` / `after`), `to_type`(`before` / `after`) | 중간전환 기능(MUST HAVE) 실사용 검증 |
| Profile-03-01 | `journey_switch_cancel` | 전환 모달에서 '돌아가기' 클릭 | - | 전환 취소율, 전환 UX 진단 |
| Profile-03-02 | `logout_complete` | 로그아웃 클릭 성공 | - | 로그아웃 행동 빈도 (참고용) |
| Profile-03-03 | `account_delete_complete` | 회원탈퇴 처리 성공 | - | 회원 탈퇴율, 정성 인터뷰 대상자 트리거 |